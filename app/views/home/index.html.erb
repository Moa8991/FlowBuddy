<div class="container text-center mt-5">
  <h1 class="display-4 fw-bold">FlowBuddy</h1>
  <p class="lead mt-3">考えなくても動ける、ルーティーン補助アプリ</p>

  <% if user_signed_in? %>
    <div class="mt-4">
      <%= link_to 'マイルーティーンを見る', routines_path, class: 'btn btn-primary btn-lg' %>
    </div>
  <% else %>
    <div class="mt-4">
      <%= link_to 'ログイン', new_user_session_path, class: 'btn btn-outline-primary btn-lg me-2' %>
      <%= link_to '新規登録', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
    </div>
  <% end %>
</div>