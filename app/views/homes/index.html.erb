<div class="min-h-screen bg-white flex flex-col items-center justify-center px-4 text-center">
  <h1 class="text-4xl sm:text-5xl font-bold text-gray-900 mb-4">FlowBuddy</h1>
  <p class="text-lg sm:text-xl text-gray-600 mb-8">考えなくても動ける、ルーティーン補助アプリ</p>

  <% if user_signed_in? %>
    <%= link_to 'ルーティーン一覧へ', routines_path, class: 'px-6 py-3 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-100 transition' %>
    <%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete } %>
  <% else %>
    <div class="flex gap-4">
      <%= link_to 'ログイン', new_user_session_path, class: 'px-6 py-3 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-100 transition' %>
      <%= link_to '新規登録', new_user_registration_path, class: 'px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition' %>
    </div>
  <% end %>
</div>
